{
  "private": true,
  "dependencies": {
    "debounce": "^1.1.0",
    "journalstate": "0.0.7",
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.0"
  },
  "devDependencies": {
    "browserify": "^16.2.0",
    "browserify-incremental": "^3.1.1"
  },
  "scripts": {
    "watch": "ls js/*.js | entr browserifyinc -vd js/app.js -o bundle.js",
    "deploy": "netlify deploy"
  }
}
